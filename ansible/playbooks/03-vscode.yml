---
# ============================================================
# VS CODE STACK DEPLOYMENT
# single-container fallback when vscode_users is empty
# multi-tenant when vscode_users is defined
# ============================================================

- name: Deploy VS Code
  hosts: vscode_servers
  become: true

  roles:
    - role: vscode
