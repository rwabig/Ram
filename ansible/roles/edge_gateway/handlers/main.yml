---
# ============================================================
# EDGE GATEWAY — HANDLERS
# ============================================================

- name: Reload edge nginx
  shell: |
    set -e
    docker exec edge-nginx nginx -t
    docker exec edge-nginx nginx -s reload
  args:
    executable: /bin/bash
  changed_when: false
